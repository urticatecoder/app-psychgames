# Node docker image on which this would be run
image: node:latest

cache:
  paths:
    - node_modules/

stages:
  - test
  - deploy

Test:
  stage: test
  script:
    - npm install
    - npm run test
    - npm run test-coverage

Deploy:
  stage: deploy
  script:
    - echo 'hello world'
  only:
    - xipu
